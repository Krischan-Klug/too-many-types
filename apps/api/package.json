{
  "name": "api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "dotenv -e ../../.env -- tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "dotenv -e ../../.env -- node dist/index.js",
    "prisma:generate": "dotenv -e ../../.env -- prisma generate",
    "prisma:migrate": "dotenv -e ../../.env -- prisma migrate dev --name init"
  },
  "dependencies": {
    "@fastify/cors": "^10",
    "@fastify/jwt": "^9",
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^2",
    "fastify": "^5",
    "zod": "^3",
    "example-module": "file:../../packages/example-module"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/node": "^24.3.1",
    "dotenv-cli": "^7",
    "prisma": "^5.22.0",
    "tsx": "^4",
    "typescript": "^5"
  }
}
